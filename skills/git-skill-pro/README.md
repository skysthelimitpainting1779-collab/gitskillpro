# GitSkillPro Agent Skill

`SKILL.md` is the portable judgment layer. It stays concise and delegates deterministic inspection, policy, evidence, and audit behavior to the shared GitSkillPro core/CLI as those surfaces become available.

The cumulative root specifications define the full target product; the currently shipped implementation may expose a smaller proven capability set.
